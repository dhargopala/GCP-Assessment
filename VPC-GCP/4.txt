Steps to perform SSH onto the private network instance:

1.	Open SSH Terminal of public instance
2.	Type ssh private-instance-name (we’re in the ssh terminal of private instance now)
3.	Type sudo apt-get install nginx
